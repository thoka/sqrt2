# pnpm-Workspace-Konfiguration (pnpm 11).
#
# allowBuilds: Build-Skripte, die pnpm aus Sicherheitsgruenden per Default
# blockiert, hier explizit erlauben. Ohne esbuild-Postinstall fehlt das
# esbuild-Binary und `vite build` (Schritt 6 von setup-env.sh) schlaegt fehl
# (ERR_PNPM_IGNORED_BUILDS). In pnpm 11 heisst das Feld `allowBuilds`
# (Vorgänger-Versionen nutzten `onlyBuiltDependencies`).
#
# minimumReleaseAge: 0 -> die Supply-Chain-Alterspruefung fuer dieses Projekt
# deaktivieren. Das Projekt nutzt bewusst aktuelle Versionen (z.B.
# svelte@5.56.5 gilt als "zu frisch" fuer die Default-Cutoff und triggert
# ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION). Wer die Policy behalten will, kann
# stattdessen gezielt `minimumReleaseAgeExclude: [svelte@5.56.5]` setzen.
# es5-ext kam als transitive Dependency von svelte-i18n (intl-messageformat-
# Kette) hinzu - Postinstall ist reines JS (Cache-Erzeugung fuer ES5/ES6-
# Ponyfills, keine native Kompilierung), daher wie esbuild erlaubt.
allowBuilds:
  es5-ext: true
  esbuild: true
minimumReleaseAge: 0
