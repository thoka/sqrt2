node_modules/
dist/
.vite/
playwright-report/
test-results/
e2e/artifacts/
.direnv/
.claude/settings.local.json

# npm-Rest (Projekt nutzt pnpm - siehe AGENTS.md) nicht zurückkriechen lassen
package-lock.json

# Relay-/Exponat-Server-Laufzeitdaten (admin_key etc.) + Relay-PID/Log
data/
.relay.pid
.relay.log

# Pro-Klon lokale Ports (von scripts/init-local-ports.sh)
.ports.local.env

# Secrets (API_KEYS/ADMIN_KEY/...) - siehe deploy/.env.example als Vorlage.
# NIE committen. .ports.local.env (oben) ist explizit KEIN Secret, daher
# eigenes Muster statt eines allgemeinen "*.env".
.env
deploy/.env

# Debug-Inspect-Kanal Artefakte
/debug/
